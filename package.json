{
  "name": "adisseo",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix adiplan-ai",
    "dev": "npm run dev --prefix adiplan-ai",
    "lint": "npm run lint --prefix adiplan-ai",
    "start": "npm run start --prefix adiplan-ai"
  },
  "dependencies": {
    "pdf-parse": "^2.4.5"
  },
  "devDependencies": {
    "@types/pdf-parse": "^1.1.5"
  }
}
